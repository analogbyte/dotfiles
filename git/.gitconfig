[user]
    email = code@danieln.de
    name = Daniel Nägele
[alias]
    ci = commit -v
    co = checkout
    a = add
    p = pull --ff-only --prune
    s = status -sb
    lv = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --numstat
    lg = log --graph --pretty=format:"%C(yellow)%h\\ %ad%Cred%d\\ %Creset%s%Cblue\\ [%G?:\\ %cn]" --decorate --date=relative
    grep = grep -Ii
    ready = rebase -i @{u}
    undo = "!gitundo() { git reset --hard \"@{${1-1}}\"; }; gitundo"
    delbr = "!git branch --merged | grep  -v '\\*\\|master\\|develop' | xargs -n 1 git branch -d"
[diff]
    tool = vimdiff
[push]
    default = current
[color]
    ui = true
[http "https://gopkg.in"]
	followRedirects = true
